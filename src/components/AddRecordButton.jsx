const AddRecordButton=({addArtist})=>{
    return(
        <div>
            <span colSpan="4">
            <button onClick={addArtist}>Add</button>
            </span>
        </div>
    )
}

export default AddRecordButton